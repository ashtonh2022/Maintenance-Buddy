import { supabase } from "@/lib/supabase";
import { timelineEntryRow, timelineEntryInsert, timelineEntryUpdate } from "@/types/types";

export const addtimelineEntry = (timelineEntry: timelineEntryInsert) => {}
export const gettimelineEntry = (timelineEntry: timelineEntryRow) => {}
export const updatetimelineEntry = (timelineEntry: timelineEntryUpdate) => {}
export const deletetimelineEntry = () => {}