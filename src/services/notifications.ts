import { supabase } from "@/lib/supabase";
import { NotificationRow, NotificationInsert, NotificationUpdate } from "@/types/types";

export const getnotification = (notification: NotificationRow) => {}
export const updatenotification = (notification: NotificationUpdate) => {}
