import { supabase } from "@/lib/supabase";
import { ScheduleRow, ScheduleInsert, ScheduleUpdate } from "@/types/types";

export const addschedule = (schedule: ScheduleInsert) => {}
export const getschedule = (schedule: ScheduleRow) => {}
export const updateschedule = (schedule: ScheduleUpdate) => {}